// import React from 'react';
/*import { GoogleMap, useJsApiLoader } from '@react-google-maps/api';
import styles from '@/app/ui/contacts/contacts.module.css';

const defaultMapCenter = {
  lat: 50.393063906992666,
  lng: 30.486866010159417,
};
const defaultMapZoom = 18;

export default function TestGoogleMap() {
  const { isLoaded } = useJsApiLoader({
    // id: 'google-map-script',
    googleMapsApiKey: process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY,
  });

  console.log('is map loaded:', isLoaded);

  // const [map, setMap] = React.useState(null);

  // const onLoad = React.useCallback(function callback(map) {

  //   const bounds = new window.google.maps.LatLngBounds(center);
  //   map.fitBounds(bounds);

  //   setMap(map);
  // }, []);

  // const onUnmount = React.useCallback(function callback(map) {
  //   setMap(null);
  // }, []);

  return isLoaded ? (
    <GoogleMap
      // mapContainerStyle={containerStyle}
      mapContainerClassName={styles['location-map']}
      center={defaultMapCenter}
      zoom={defaultMapZoom}></GoogleMap>
  ) : (
    <></>
  );
}

*/
